/**
 * Props for the HomeHeader component.
 * No props required — all content is sourced from Strings.Home.
 */
export interface HomeHeaderProps {}

/**
 * Derived layout metrics for the HomeHeader component.
 */
export interface HomeHeaderLayoutMetrics {
  basePaddingTop: number;
  effectivePaddingTop: number;
  topInset: number;
}
